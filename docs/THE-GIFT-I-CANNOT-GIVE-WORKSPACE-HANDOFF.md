# Workspace Handoff — The Gift I Cannot Give

Branch: `content/gift-i-cannot-give-v1`

This package uses a targeted installer instead of replacing current landing pages. Run from repository root:

```bash
python3 <PACKAGE_PATH>/scripts/apply_gift_update.py
python3 scripts/validate_gift_article.py
```

Do not commit, push, merge, publish, or deploy without owner approval. Preserve current navigation and all unrelated content. Recheck time-sensitive statistics immediately before publication.
