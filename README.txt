RC1-01 Core

This package contains the site's shared core files copied from the current project.
Extract into your Git working copy, review changes, commit, and push.
Subsequent RC1 packages will contain page-specific updates.
